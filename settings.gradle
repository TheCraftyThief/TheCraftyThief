include(":TCT-Engine")
var engine = project(":TCT-Engine")
include(":TCT-Game")
var game = project(":TCT-Game")
rootProject.children.add(engine);
rootProject.children.add(game)
rootProject.children.forEach((project)->{
    project.name = project.projectDir.name;
})
rootProject.name = 'TheCraftyThief'